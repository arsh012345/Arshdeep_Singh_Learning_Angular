import { HHighlightDirective } from './h-highlight.directive';

describe('HHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
